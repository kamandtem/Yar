# اعلان‌های روزانه یار

- بعد از پایان راه‌اندازی اولیه، یک رابط توضیح مجوز نمایش داده می‌شود و سپس مجوز بومی Android درخواست می‌شود.
- هر روز دو جمله متفاوت و کوتاه، ساعت ۱۰:۰۰ و ۲۰:۳۰ زمان‌بندی می‌شوند.
- جمله‌ها برای هر تاریخ ثابت‌اند تا اعلان سیستم و پنل زنگوله یک متن نشان دهند.
- زمان‌بندی ۲۱ روز آینده با هر بار اجرای برنامه تازه می‌شود.
- لمس اعلان کاربر را به خانه یار می‌برد.
- کنترل روشن و خاموش کردن اعلان‌ها در تنظیمات اضافه شده است.

پس از دریافت وابستگی‌ها، `npm install` و سپس `npm run cap:sync` را اجرا کنید.

- آیکون دو جمله روزانه در زنگوله به قلب داخل زنگوله تغییر کرد.
- هشدار خروج کاملاً داخلی شد و دیگر از window.alert قدیمی استفاده نمی‌کند.

- کارت اقلیم عاطفی بر اساس دمای ثبت‌شده بین هوای ابری و آفتابی تغییر می‌کند.
- در دمای پایین، مقاله‌های ترمیمی و کاهش تنش اولویت می‌گیرند.
- راهنمای اقلیم فقط یک بار بعد از نصب و پایان راه‌اندازی نمایش داده می‌شود.
