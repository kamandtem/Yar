# تغییرات این نسخه (رابط کاربری)

## ۱. اپ دیگر روی نوار وضعیت گوشی نمی‌افتد
- در `capacitor.config.ts` گزینهٔ `adjustMarginsForEdgeToEdge: 'force'` فعال شد.
  از اندروید ۱۵ به بعد WebView به‌صورت پیش‌فرض تمام‌صفحه (edge-to-edge) می‌شود و ساعت/باتری/آنتن
  روی محتوای اپ می‌افتاد؛ این گزینه حاشیهٔ سیستمی را به WebView اعمال می‌کند.
- پلاگین `@capacitor/status-bar` اضافه شد: رنگ نوار وضعیت و روشن/تیره بودن آیکون‌هایش با تم اپ
  هم‌رنگ می‌شود (`src/native/systemBars.ts` و همگام با کلید حالت شب).
- `scripts/patch-android.mjs` روی پروژهٔ اندروید تولیدشده این‌ها را ست می‌کند:
  اسم فارسی «یار»، `windowLightStatusBar` (آیکون‌های تیره روی نوار روشن) و رنگ پس‌زمینهٔ پنجره.
- در CSS متغیرهای `--yar-safe-top` / `--yar-safe-bottom` بر پایهٔ `env(safe-area-inset-*)` اضافه شد
  و `viewport-fit=cover` به `index.html` رفت، پس روی گوشی‌های ناچ‌دار هم درست می‌نشیند.

## ۲. هدر شناور
`src/components/layout/Navbar.tsx` از حالت چسبیده به بالای صفحه به یک کارت شناور تبدیل شد:
گوشه‌های گرد ۲۶px، پس‌زمینهٔ نیمه‌شفاف با بلور، سایهٔ نرم، فاصله از لبه‌ها و از نوار وضعیت.
چیدمان راست‌به‌چپ: دکمهٔ همبرگری منو + آیکون اپ + عنوان «یار» در راست، و SOS، شمارندهٔ استمرار و
کلید شب/روز در چپ.

## ۳. نوار پایین شناور + دکمهٔ گرد جدا
`src/components/layout/BottomNav.tsx` بازنویسی شد: یک نوار کپسولی شناور با چهار تب که تب فعال
داخل یک کپسول روشن با آیکون و برچسب می‌نشیند (انیمیشن جابه‌جایی با `layoutId`)، و دکمهٔ گرد
«فضای دونفره» به شکل FAB بیرون از نوار، دقیقاً مثل نمونه‌ای که فرستادی.

## ۴. منوی کناری از سمت راست
`src/components/layout/SideDrawerMenu.tsx`:
- قبلاً با `justify-end` چیده شده بود که در حالت RTL منو را به **چپ** می‌برد. حالا با
  `right-0` صریحاً به لبهٔ راست چسبیده و همان‌جا می‌ماند.
- باز و بسته شدن با انیمیشن اسپرینگ و `AnimatePresence` (قبلاً فقط باز شدن انیمیشن داشت).
- کشیدنی شد: با درگ کردن منو به سمت راست (بیش از ۸۰px یا با سرعت کافی) پنهان می‌شود،
  همراه با حس کشسان. کلیک روی پس‌زمینه و کلید Esc هم مثل قبل کار می‌کند.
- اسکرول پس‌زمینه هنگام باز بودن منو قفل می‌شود.

## ۵. فونت دانا
- سه فایل `Dana-Light/Regular/Bold.woff2` در `src/fonts/` قرار گرفت و با `@font-face` داخل باندل
  می‌آید، پس **آفلاین** هم درست نمایش داده می‌شود.
- لینک Google Fonts (وزیرمتن) از `index.html` حذف شد؛ قبلاً بدون اینترنت فونت فارسی به فونت
  پیش‌فرض سیستم برمی‌گشت.
- وزن‌ها: ۳۰۰ روی Light، ۴۰۰–۵۰۰ روی Regular، ۶۰۰–۹۰۰ روی Bold. یعنی `font-bold` و
  `font-black` هم فایل واقعی Bold را می‌گیرند و مرورگر بولد مصنوعی نمی‌سازد.
- `--font-sans` تم Tailwind هم روی دانا ست شد.

## ۶. فاصله‌گذاری محتوا
کلاس `.yar-main` فضای لازم بالای صفحه (زیر هدر شناور) و پایین صفحه (روی نوار شناور + نوار
حرکتی گوشی) را می‌سازد، پس دیگر لازم نیست هر تب خودش `pb-28` داشته باشد.
